import {StyleSheet} from 'react-native';
import {colors} from '../../styles/colors';

export const styles = StyleSheet.create({
  RemindersScreen: {
    flex: 1,
    backgroundColor: colors.lightDark,
  },

  RemindersScreen__content: {
    padding: 20,
  },
});
